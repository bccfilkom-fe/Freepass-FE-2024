export interface FollowedArtist {
  artists: {
    href: string;
    limit: 0;
    next: string;
    cursors: {
      after: string;
      before: string;
    };
    total: 0;
    items: [
      
    ];
  };
}
